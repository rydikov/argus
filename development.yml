app:
  state_dir: /app/state

sources:
  first-cam:
    source: /app/res/demo.mov
    save_every_sec: 3600
    stills_dir: /app/Stills
    host_stills_uri: http://localhost/Stills/
    important_objects:
      - person
      - car

recognizer:
  device_name: CPU
  num_requests: 4
